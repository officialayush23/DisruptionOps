# Visual prompts — DisruptionOps deck

Everything the review asked for is already drawn in the .pptx with native shapes,
so the deck is complete as delivered. These prompts are only for the places where
a real render or a real screenshot would beat a drawing. Each one names the slide
it replaces on, so you can drop the image straight in.

Two rules that keep whatever you generate inside the deck's design system:

- **Palette:** deep maroon `#7A1128`, gold `#C9993F`, ink `#14191C`, body grey
  `#3E4A50`, label grey `#6B7980`, hairline `#D3DAD9`, panel `#FAFBFA`,
  water `#DCE9F2`. Severity colours: 5 `#B31B1B`, 4 `#C15A1E`, 3 `#C9993F`,
  2 `#2E6E9E`.
- **Type:** Cambria for headings, Calibri for everything else. Flat, no gradients,
  no drop shadows, no glow, hairline borders only.

---

## 1 — Slide 2 · the operational map (optional upgrade)

The slide already has a schematic map. Replace it only if you want a real one.

**Best option, no AI needed:** screenshot your own deployed map at
`https://distro-ruddy.vercel.app/` with the five demo zones live, crop to
7.55 × 3.10 inches, and drop it in place of the white panel. A real screenshot of
your own system beats any generated picture in front of judges.

**If you want it generated instead:**

> A clean, flat vector-style map illustration of an Indian city district during
> monsoon flooding, viewed from directly above. Muted off-white land, a pale blue
> river `#DCE9F2` curving diagonally across, thin light-grey road lines. Five
> circular location pins placed apart from each other, coloured `#B31B1B`,
> `#C15A1E`, `#C9993F`, `#C15A1E`, `#2E6E9E`, each with a white numeral inside.
> No text labels, no legend, no 3D, no shadows, no photorealism. Minimal
> editorial infographic style, very light background, hairline strokes.
> 16:7 aspect ratio.

---

## 2 — Slide 6 / 7 · the system architecture

You are drawing these yourself from `DisruptionOps-system-architecture.md`, and
they are already placed in the deck. Nothing needed here.

---

## 3 — Slide 9 · the three surfaces (strongly recommended: real screenshots)

The deck currently has drawn device mockups. Real screenshots of your own three
surfaces will land much harder. Take these three and I can swap them in:

1. **Phone** — `/citizen` on a phone-width window, map visible with the route
   drawn and at least one zone at severity 5.
2. **Desktop** — the command console showing the risk board or the allocation
   planner, with the incident queue visible on the right.
3. **Tablet or phone** — the field crew app on an accepted task, showing the
   accept → on site → close row.

Crop each to just the browser content (no address bar, no OS chrome), and keep
them roughly 4:3, 16:10 and 4:3 respectively. Send them over and I will put them
inside the device frames that are already on the slide.

**If you would rather generate a placeholder:**

> Three flat vector UI mockups side by side on a white background: a phone
> showing a map with a red route line and a maroon "REPORT" button; a wide
> desktop dashboard with a left sidebar, a central pale-blue map with four
> coloured incident pins, and a right-hand list panel; a tablet showing a single
> task card with a green ACCEPT button. Deep maroon `#7A1128` accents, grey
> hairline borders, no gradients, no shadows, no text that must be legible.
> Clean editorial product-illustration style. 16:6 aspect ratio.

---

## 4 — Slide 10 · the Copilot (strongly recommended: a real screenshot)

The slide draws a Copilot conversation. A real one is far more convincing, and
you have the system running.

Capture this exact sequence in the console Copilot, then screenshot the whole
panel:

1. "What are my options for the next three hours?"
2. "What if I move Pump 3 to Baner?"
3. "Apply the second one."

Crop to the Copilot panel only, roughly 3:2. Send it and I will drop it into the
left-hand window on the slide, keeping the WHAT-IF panel beside it.

**If you would rather generate a placeholder:**

> A flat vector mockup of a dark-themed AI assistant chat panel on a deep brown
> `#241A10` background. A gold header bar, two lighter user message bubbles on
> the right, two darker response cards on the left containing short rows of
> cream-coloured text, and a text input row at the bottom with a small maroon
> circular send button. Cream `#F5EBD2` text, gold `#C9993F` labels, thin
> `#3A2A18` borders, no gradients, no glow, no photorealism. 3:2 aspect ratio.

---

## 5 — Slide 1 · title-slide art (only if you want to replace the current mark)

> A flat editorial illustration of a city grid seen from above, partly submerged,
> with a small cluster of response vehicles moving along one clear route through
> it. Deep maroon `#7A1128` and gold `#C9993F` on an off-white ground, pale blue
> `#DCE9F2` for water. Minimal, geometric, no people's faces, no text, no logos,
> no 3D rendering, no dramatic lighting. Square.

---

## What I would actually do

Ship the deck as it is — every visual the review asked for is drawn natively,
which means it scales, prints and edits cleanly. Then, if there is time before
you submit, take the four real screenshots (three surfaces + Copilot) and send
them across. Those are the only images that will beat what is already on the
slides, because they are proof rather than illustration.
